---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const allDocs = await getCollection('docs', ({ data }) => {
  return import.meta.env.PROD ? data.draft !== true : true;
});
const recentDocs = allDocs.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()).slice(0, 3);

const allBlog = await getCollection('blog', ({ data }) => {
  return import.meta.env.PROD ? data.draft !== true : true;
});
const recentBlog = allBlog.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()).slice(0, 3);

const skills = {
  languages: [
    { name: 'Java', level: 95, years: 8 },
    { name: 'TypeScript', level: 92, years: 5 },
    { name: 'JavaScript', level: 90, years: 7 },
    { name: 'Python', level: 78, years: 3 },
    { name: 'SQL', level: 88, years: 6 },
    { name: 'HTML/CSS', level: 94, years: 7 },
  ],
  frontend: ['React', 'Vue.js', 'Astro', 'Next.js', 'Tailwind CSS', 'Svelte'],
  backend: ['Spring Boot', 'Node.js', 'Express.js', 'NestJS', 'GraphQL', 'gRPC'],
  devops: ['Docker', 'Kubernetes', 'GitHub Actions', 'Jenkins', 'AWS', 'Terraform'],
  databases: ['PostgreSQL', 'MongoDB', 'Redis', 'MySQL', 'Elasticsearch'],
};

const projects = [
  { title: 'Cloud Native Platform', type: 'Full Stack', desc: 'Microservices-based enterprise platform with auto-scaling, service mesh, and real-time monitoring dashboard.', tags: ['Java', 'Spring Boot', 'Kubernetes', 'React'], link: '#', github: 'https://github.com/mk100120' },
  { title: 'AI Code Review Bot', type: 'AI/ML', desc: 'Automated code review tool using LLM to analyze PRs, detect anti-patterns, and suggest improvements.', tags: ['Python', 'OpenAI', 'TypeScript', 'GitHub API'], link: '#', github: 'https://github.com/mk100120' },
  { title: 'Real-time Analytics Engine', type: 'Backend', desc: 'High-throughput event processing pipeline handling 100K+ events/sec with sub-second query latency.', tags: ['Java', 'Kafka', 'ClickHouse', 'Grafana'], link: '#', github: 'https://github.com/mk100120' },
  { title: 'DevOps Dashboard', type: 'Web App', desc: 'Unified dashboard for CI/CD pipelines, deployment status, and infrastructure health monitoring.', tags: ['React', 'Node.js', 'Docker', 'WebSocket'], link: '#', github: 'https://github.com/mk100120' },
  { title: 'E-Commerce Storefront', type: 'Full Stack', desc: 'Headless e-commerce platform with SSR, dynamic filtering, payment integration, and inventory management.', tags: ['Next.js', 'Stripe', 'PostgreSQL', 'Redis'], link: '#', github: 'https://github.com/mk100120' },
  { title: 'Knowledge Base System', type: 'Open Source', desc: 'This portfolio site built with Astro, featuring content collections, tag filtering, and glassmorphism UI.', tags: ['Astro', 'TypeScript', 'MDX', 'CSS'], link: '/', github: 'https://github.com/mk100120' },
];

const experience = [
  { role: 'Senior Software Engineer', company: 'Tech Corp', period: 'Mar 2023 - Present', location: 'Shanghai, China', type: 'Full-time', current: true, desc: 'Leading backend architecture and cloud-native transformation for enterprise SaaS platform.', achievements: ['Architected microservices migration reducing deployment time by 60% and improving system reliability to 99.95% uptime', 'Led a team of 6 engineers delivering Spring Boot + Kubernetes platform serving 2M+ daily requests', 'Designed event-driven architecture with Kafka processing 100K+ events/sec with sub-100ms latency', 'Implemented CI/CD pipelines with GitHub Actions achieving 15-minute build-to-production cycles'], tags: ['Java', 'Spring Boot', 'Kubernetes', 'Kafka', 'PostgreSQL', 'AWS'] },
  { role: 'Full Stack Developer', company: 'Digital Studio', period: 'Jun 2021 - Feb 2023', location: 'Shanghai, China', type: 'Full-time', current: false, desc: 'Built modern web applications and internal tools for enterprise clients.', achievements: ['Delivered 8 production web applications with React/Next.js achieving 95+ Lighthouse scores', 'Built real-time collaboration features using WebSocket serving 500+ concurrent users', 'Reduced API response times by 70% through query optimization and Redis caching strategies', 'Mentored 3 junior developers and established code review practices for the team'], tags: ['React', 'Next.js', 'Node.js', 'TypeScript', 'MongoDB', 'Docker'] },
  { role: 'Backend Developer', company: 'StartupX', period: 'Sep 2019 - May 2021', location: 'Beijing, China', type: 'Full-time', current: false, desc: 'Core backend developer for fintech startup building payment processing systems.', achievements: ['Built RESTful APIs handling 50K+ daily transactions with zero downtime', 'Implemented JWT-based auth system with role-based access control for 10K+ users', 'Designed database schema and optimized PostgreSQL queries reducing p95 latency from 300ms to 45ms', 'Integrated third-party payment gateways (Alipay, WeChat Pay) processing $2M+ monthly'], tags: ['Java', 'Spring Boot', 'PostgreSQL', 'Redis', 'Docker', 'RabbitMQ'] },
  { role: 'Junior Developer', company: 'Web Agency', period: 'Jul 2017 - Aug 2019', location: 'Hangzhou, China', type: 'Full-time', current: false, desc: 'Developed responsive websites and web applications for local businesses.', achievements: ['Developed 15+ responsive websites using Vue.js, PHP, and MySQL', 'Built custom CMS reducing client content update time by 80%', 'Created reusable component library used across 10+ projects'], tags: ['Vue.js', 'PHP', 'MySQL', 'JavaScript', 'jQuery'] },
];

const contacts = [
  { label: 'Email', value: 'kenny.dev@example.com', url: 'mailto:kenny.dev@example.com' },
  { label: 'Location', value: 'Shanghai, China', url: null },
];

const socials = [
  { name: 'GitHub', url: 'https://github.com/mk100120', icon: 'M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z' },
  { name: 'Twitter', url: 'https://x.com', icon: 'M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z' },
  { name: 'LinkedIn', url: 'https://linkedin.com', icon: 'M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z' },
];

const heroWords = "Engineering Tomorrow's Digital Frontier".split(' ');
---

<BaseLayout title="Kenny | Full Stack Developer | Portfolio">

  <!-- ===== HOME ===== -->
  <section id="home" class="hero">
    <div class="container hero-content">
      <div class="scroll-fade-up">
        <span class="section-badge">Available for opportunities</span>
      </div>

      <p class="hero-greeting scroll-fade-up scroll-fade-up-delay-100">Hi, I'm</p>

      <h1 class="hero-title words-pull-up">
        {heroWords.map((word, i) => (
          <span class="word" style={`--delay: ${i * 0.08}s`}>
            {i >= heroWords.length - 2
              ? <span class="gradient-text">{word}</span>
              : word}
          </span>
        ))}
      </h1>

      <h2 class="hero-role scroll-fade-up scroll-fade-up-delay-200">Full Stack Developer</h2>

      <p class="hero-desc scroll-fade-up scroll-fade-up-delay-300">
        Passionate developer creating amazing digital experiences.
      </p>

      <div class="hero-pills scroll-fade-up scroll-fade-up-delay-300">
        <span class="hero-pill">Clean Code</span>
        <span class="hero-pill">Modern UI/UX</span>
        <span class="hero-pill">Fast Performance</span>
      </div>

      <div class="hero-actions scroll-fade-up scroll-fade-up-delay-400">
        <a href="#projects" class="btn-gradient">
          View My Work
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
        </a>
        <a href="/resume.pdf" class="btn-ghost">
          Download Resume
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
        </a>
      </div>

      <div class="hero-socials scroll-fade-up scroll-fade-up-delay-500">
        {socials.map((s) => (
          <a href={s.url} class="social-icon" target="_blank" rel="noopener" title={s.name}>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d={s.icon}/></svg>
          </a>
        ))}
      </div>

      <div class="scroll-hint scroll-fade-up scroll-fade-up-delay-500">
        <span>Scroll to explore</span>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg>
      </div>
    </div>
  </section>

  <!-- ===== ABOUT ===== -->
  <section id="about" class="section">
    <div class="container">
      <div class="scroll-fade-up">
        <span class="section-badge">About Me</span>
        <h2 class="section-heading">Professional Background</h2>
        <p class="section-sub">Delivering enterprise-grade solutions with expertise in modern technologies</p>
      </div>

      <div class="about-grid">
        <div class="about-text">
          <p class="about-para scroll-fade-up">
            A seasoned full-stack developer with 7+ years of professional experience in designing, developing, and deploying scalable web applications. Specialized in Java/Spring Boot backend systems and modern React frontends.
          </p>
          <p class="about-para scroll-fade-up scroll-fade-up-delay-100">
            My expertise spans cloud-native architectures, microservices design, and full-stack development. I've successfully led projects from conception to deployment, collaborating with cross-functional teams to deliver solutions that exceed expectations.
          </p>

          <div class="about-highlights">
            <div class="highlight-card glass-card scroll-fade-up">
              <h4 class="highlight-title">Full-Stack Delivery</h4>
              <p class="highlight-desc">7 years shipping Spring Boot and React applications with robust API layers and cloud deployments.</p>
            </div>
            <div class="highlight-card glass-card scroll-fade-up scroll-fade-up-delay-100">
              <h4 class="highlight-title">Performance Engineering</h4>
              <p class="highlight-desc">Reduced API latency by 70% and improved system throughput handling 100K+ events/sec in production.</p>
            </div>
            <div class="highlight-card glass-card scroll-fade-up scroll-fade-up-delay-200">
              <h4 class="highlight-title">Team Leadership</h4>
              <p class="highlight-desc">Led engineering teams of 6+ in Agile environments, mentoring juniors and establishing best practices.</p>
            </div>
          </div>
        </div>

        <div class="about-sidebar scroll-fade-up scroll-fade-up-delay-200">
          <div class="profile-card glass-strong">
            <div class="profile-avatar">K</div>
            <div class="profile-status">
              <span class="status-dot"></span>
              <span>Remote Ready</span>
            </div>
            <h4 class="profile-name">Open for Opportunities</h4>
            <div class="profile-types">
              <span class="tag">Freelance</span>
              <span class="tag">Contract</span>
              <span class="tag">Full-time</span>
            </div>

            <div class="profile-stats">
              <div class="p-stat"><span class="p-stat-value">7+</span><span class="p-stat-label">Years Experience</span></div>
              <div class="p-stat"><span class="p-stat-value">30+</span><span class="p-stat-label">Projects Completed</span></div>
              <div class="p-stat"><span class="p-stat-value">10+</span><span class="p-stat-label">Happy Clients</span></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== SKILLS ===== -->
  <section id="skills" class="section">
    <div class="container">
      <div class="scroll-fade-up">
        <span class="section-badge">Skills & Expertise</span>
        <h2 class="section-heading">What I Bring to the Table</h2>
        <p class="section-sub">A comprehensive toolkit of modern technologies and frameworks</p>
      </div>

      <div class="skills-section">
        <h3 class="skills-cat-title scroll-fade-up">Languages</h3>
        <div class="skills-bars">
          {skills.languages.map((s, i) => (
            <div class="skill-bar-card glass-card scroll-fade-up" style={`--stagger-delay: ${i * 100}`}>
              <div class="skill-bar-header">
                <span class="skill-bar-name">{s.name}</span>
                <span class="skill-bar-years">{s.years} years</span>
              </div>
              <div class="skill-bar-track">
                <div class="skill-bar-fill" style={`--fill: ${s.level}%`}></div>
              </div>
              <span class="skill-bar-pct">{s.level}%</span>
            </div>
          ))}
        </div>
      </div>

      <div class="skills-tags-grid">
        {Object.entries({ 'Frontend': skills.frontend, 'Backend': skills.backend, 'DevOps & Cloud': skills.devops, 'Databases': skills.databases }).map(([cat, items]) => (
          <div class="skills-tag-group scroll-fade-up">
            <h4 class="skills-tag-cat">{cat}</h4>
            <div class="skills-tag-list">
              {items.map((t) => <span class="tag">{t}</span>)}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== PROJECTS ===== -->
  <section id="projects" class="section">
    <div class="container">
      <div class="scroll-fade-up">
        <span class="section-badge">Portfolio</span>
        <h2 class="section-heading">Featured Projects</h2>
        <p class="section-sub">A showcase of work demonstrating creativity and technical excellence</p>
      </div>

      <div class="projects-grid">
        {projects.map((p, i) => (
          <div class="project-card glass-card card-glow scroll-fade-up" style={`--stagger-delay: ${i * 120}`}>
            <div class="project-type">{p.type}</div>
            <h3 class="project-title">{p.title}</h3>
            <p class="project-desc">{p.desc}</p>
            <div class="project-tags">
              {p.tags.map((t) => <span class="tag">{t}</span>)}
            </div>
            <div class="project-links">
              {p.link !== '#' && (
                <a href={p.link} class="project-link" target="_blank" rel="noopener">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
                  Live
                </a>
              )}
              <a href={p.github} class="project-link" target="_blank" rel="noopener">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
                Code
              </a>
            </div>
          </div>
        ))}
      </div>

      <div class="projects-more scroll-fade-up">
        <a href="https://github.com/mk100120" class="btn-ghost" target="_blank" rel="noopener">
          Explore More on GitHub
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 7h10v10"/><path d="M7 17 17 7"/></svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ===== BLOG ===== -->
  <section id="blog" class="section">
    <div class="container">
      <div class="section-header-row scroll-fade-up">
        <div>
          <span class="section-badge">Blog & Docs</span>
          <h2 class="section-heading">Latest Writing</h2>
        </div>
        <div class="section-header-links">
          <a href="/blog/" class="section-link">Blog &rarr;</a>
          <a href="/docs/" class="section-link">Docs &rarr;</a>
        </div>
      </div>

      <div class="blog-grid">
        {[...recentBlog, ...recentDocs].slice(0, 4).map((post, i) => (
          <a
            href={`/${'data' in post && post.data.tags ? (allBlog.includes(post) ? 'blog' : 'docs') : 'docs'}/${post.id}/`}
            class="glass-card card-glow blog-card scroll-fade-up"
            style={`--stagger-delay: ${i * 100}`}
          >
            <time datetime={post.data.pubDate.toISOString()}>
              {post.data.pubDate.toLocaleDateString('zh-CN', { year: 'numeric', month: 'short', day: 'numeric' })}
            </time>
            <h3 class="blog-card-title">{post.data.title}</h3>
            <p class="blog-card-desc">{post.data.description}</p>
            <div class="blog-card-tags">
              {post.data.tags.slice(0, 3).map((t) => <span class="tag">{t}</span>)}
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== EXPERIENCE ===== -->
  <section id="experience" class="section">
    <div class="container">
      <div class="scroll-fade-up">
        <span class="section-badge">Career Journey</span>
        <h2 class="section-heading">Professional Experience</h2>
        <p class="section-sub">A timeline of growth, achievements, and continuous learning</p>
      </div>

      <div class="timeline">
        {experience.map((exp, i) => (
          <div class="timeline-item scroll-fade-up" style={`--stagger-delay: ${i * 150}`}>
            <div class="timeline-marker">
              <div class={`timeline-dot ${exp.current ? 'dot-current' : ''}`}></div>
              {i < experience.length - 1 && <div class="timeline-line"></div>}
            </div>
            <div class="timeline-content glass-card card-glow">
              {exp.current && <span class="current-badge">Current Position</span>}
              <h3 class="exp-role">{exp.role}</h3>
              <div class="exp-meta">
                <span class="exp-company">{exp.company}</span>
                <span class="exp-divider">|</span>
                <span>{exp.period}</span>
              </div>
              <div class="exp-meta-sub">
                <span>{exp.location}</span>
                <span class="exp-type-badge">{exp.type}</span>
              </div>
              <p class="exp-desc">{exp.desc}</p>
              <ul class="exp-achievements">
                {exp.achievements.map((a) => <li>{a}</li>)}
              </ul>
              <div class="exp-tags">
                {exp.tags.map((t) => <span class="tag">{t}</span>)}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== CONTACT ===== -->
  <section id="contact" class="section">
    <div class="container">
      <div class="scroll-fade-up">
        <span class="section-badge">Get In Touch</span>
        <h2 class="section-heading">Let's Start a Conversation</h2>
        <p class="section-sub">Have a project in mind? Let's build something amazing together</p>
      </div>

      <div class="contact-grid">
        <div class="contact-left">
          <div class="contact-cards">
            {contacts.map((c, i) => (
              <a href={c.url || '#'} class="glass-card contact-info-card scroll-fade-up" style={`--stagger-delay: ${i * 100}`}>
                <span class="contact-card-label">{c.label}</span>
                <span class="contact-card-value">{c.value}</span>
              </a>
            ))}
          </div>

          <div class="contact-socials scroll-fade-up scroll-fade-up-delay-200">
            <h4 class="contact-socials-title">Connect With Me</h4>
            <div class="contact-social-icons">
              {socials.map((s) => (
                <a href={s.url} class="social-icon-lg" target="_blank" rel="noopener" title={s.name}>
                  <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d={s.icon}/></svg>
                </a>
              ))}
            </div>
          </div>

          <div class="why-card glass-strong scroll-fade-up scroll-fade-up-delay-300">
            <h4 class="why-title">Why Work With Me?</h4>
            <ul class="why-list">
              <li>Fast response time (usually within 24 hours)</li>
              <li>Clear communication throughout the project</li>
              <li>Flexible and adaptable to your needs</li>
              <li>Commitment to quality and deadlines</li>
            </ul>
          </div>
        </div>

        <div class="contact-right">
          <div class="glass-strong contact-form-card scroll-fade-up scroll-fade-up-delay-100">
            <h3 class="form-title">Send Me a Message</h3>
            <p class="form-desc">Have a project in mind? Let's build something amazing together</p>
            <form class="contact-form" id="contact-form">
              <div class="form-field">
                <label for="name">Your Name</label>
                <input type="text" id="name" required autocomplete="name" placeholder="John Doe" />
              </div>
              <div class="form-field">
                <label for="email">Your Email</label>
                <input type="email" id="email" required autocomplete="email" placeholder="john@example.com" />
              </div>
              <div class="form-field">
                <label for="subject">Subject</label>
                <input type="text" id="subject" required placeholder="Project Inquiry" />
              </div>
              <div class="form-field">
                <label for="message">Message</label>
                <textarea id="message" rows="5" required placeholder="Tell me about your project..."></textarea>
              </div>
              <button type="submit" class="btn-gradient form-submit">
                Send Message
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14.536 21.686a.5.5 0 0 0 .937-.024l6.5-19a.496.496 0 0 0-.635-.635l-19 6.5a.5.5 0 0 0-.024.937l7.93 3.18a2 2 0 0 1 1.112 1.11z"/><path d="m21.854 2.147-10.94 10.939"/></svg>
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: calc(var(--nav-height) + 2rem);
    padding-bottom: 4rem;
  }
  .hero-content { max-width: 780px; }
  .hero-greeting { font-size: 1.1rem; color: var(--text-secondary); margin-bottom: 0.25rem; }
  .hero-title {
    font-size: clamp(2.2rem, 6vw, 3.8rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    line-height: 1.15;
    margin: 0.5rem 0 1rem;
  }
  .hero-role {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--purple-400);
    margin-bottom: 0.75rem;
  }
  .hero-desc {
    font-size: 1rem;
    color: var(--text-secondary);
    margin-bottom: 1.25rem;
    line-height: 1.7;
  }
  .hero-pills { display: flex; gap: 0.6rem; flex-wrap: wrap; margin-bottom: 2rem; }
  .hero-pill {
    padding: 0.35rem 0.9rem;
    font-size: 0.78rem;
    font-weight: 500;
    background: var(--glass-bg-light);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-full);
    color: var(--text-secondary);
  }
  .hero-actions { display: flex; gap: 1rem; flex-wrap: wrap; margin-bottom: 2rem; }
  .hero-socials { display: flex; gap: 0.75rem; margin-bottom: 3rem; }
  .social-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: var(--radius-md);
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    color: var(--text-secondary);
    transition: all var(--transition-fast);
  }
  .social-icon:hover {
    color: var(--purple-400);
    border-color: rgba(168,85,247,0.3);
    background: rgba(168,85,247,0.1);
    transform: translateY(-2px);
  }
  .scroll-hint {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.75rem;
    color: var(--text-tertiary);
    animation: bounce-hint 2s ease-in-out infinite;
  }
  @keyframes bounce-hint { 0%,100% { transform: translateY(0); } 50% { transform: translateY(6px); } }

  .section { padding: 6rem 0; }
  .section-heading {
    font-size: 2rem;
    font-weight: 800;
    letter-spacing: -0.03em;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
  }
  .section-sub { color: var(--text-secondary); font-size: 1rem; margin-bottom: 3rem; max-width: 600px; }
  .section-header-row {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    margin-bottom: 2.5rem;
    flex-wrap: wrap;
    gap: 1rem;
  }
  .section-header-row .section-heading { margin-bottom: 0; }
  .section-header-links { display: flex; gap: 1.5rem; }
  .section-link {
    font-size: 0.9rem;
    color: var(--purple-400);
    font-weight: 500;
    transition: color var(--transition-fast);
  }
  .section-link:hover { color: var(--text-primary); }

  .about-grid { display: grid; grid-template-columns: 1fr 340px; gap: 3rem; align-items: start; }
  .about-para { color: var(--text-secondary); line-height: 1.8; margin-bottom: 1.25rem; }
  .about-highlights { display: flex; flex-direction: column; gap: 1rem; margin-top: 2rem; }
  .highlight-card { padding: 1.25rem !important; }
  .highlight-title { font-size: 1rem; font-weight: 700; margin-bottom: 0.35rem; color: var(--text-primary); }
  .highlight-desc { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.6; }
  .profile-card { padding: 2rem; text-align: center; }
  .profile-avatar {
    width: 80px; height: 80px; margin: 0 auto 1rem;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--purple-600), var(--pink-500));
    display: flex; align-items: center; justify-content: center;
    font-size: 2rem; font-weight: 800; color: white;
  }
  .profile-status {
    display: flex; align-items: center; justify-content: center; gap: 0.4rem;
    font-size: 0.8rem; color: var(--green-400); margin-bottom: 0.5rem;
  }
  .status-dot {
    width: 8px; height: 8px; border-radius: 50%; background: var(--green-400);
    animation: pulse-dot 2s ease-in-out infinite;
  }
  @keyframes pulse-dot { 0%,100% { opacity: 1; } 50% { opacity: 0.4; } }
  .profile-name { font-size: 1rem; font-weight: 700; margin-bottom: 0.75rem; }
  .profile-types { display: flex; justify-content: center; gap: 0.4rem; flex-wrap: wrap; margin-bottom: 1.5rem; }
  .profile-stats { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 0.5rem; }
  .p-stat { text-align: center; }
  .p-stat-value { display: block; font-size: 1.25rem; font-weight: 800; color: var(--text-primary); }
  .p-stat-label { font-size: 0.7rem; color: var(--text-tertiary); }

  .skills-section { margin-bottom: 3rem; }
  .skills-cat-title { font-size: 1.1rem; font-weight: 700; color: var(--text-primary); margin-bottom: 1.25rem; }
  .skills-bars { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1rem; }
  .skill-bar-card { padding: 1rem 1.25rem !important; }
  .skill-bar-header { display: flex; justify-content: space-between; margin-bottom: 0.5rem; }
  .skill-bar-name { font-weight: 600; font-size: 0.9rem; }
  .skill-bar-years { font-size: 0.75rem; color: var(--text-tertiary); }
  .skill-bar-track {
    height: 6px; border-radius: 3px; background: rgba(255,255,255,0.06);
    overflow: hidden; margin-bottom: 0.35rem;
  }
  .skill-bar-fill {
    height: 100%; border-radius: 3px; width: var(--fill);
    background: linear-gradient(90deg, var(--purple-600), var(--purple-400));
    transition: width 1s var(--ease-out);
  }
  .skill-bar-pct { font-size: 0.75rem; color: var(--purple-400); font-weight: 600; }
  .skills-tags-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: 1.5rem; }
  .skills-tag-group {}
  .skills-tag-cat { font-size: 0.85rem; font-weight: 600; color: var(--purple-400); margin-bottom: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; }
  .skills-tag-list { display: flex; flex-wrap: wrap; gap: 0.5rem; }

  .projects-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.25rem; }
  .project-card { display: flex; flex-direction: column; gap: 0.6rem; }
  .project-type { font-size: 0.72rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.06em; color: var(--purple-400); }
  .project-title {
    font-size: 1.15rem; font-weight: 700; color: var(--text-primary);
    transition: all 0.3s ease;
  }
  .project-card:hover .project-title {
    color: var(--purple-400);
    text-shadow: 0 0 20px rgba(168,85,247,0.3);
  }
  .project-desc { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.6; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden; }
  .project-tags { display: flex; flex-wrap: wrap; gap: 0.35rem; margin-top: auto; }
  .project-links { display: flex; gap: 0.75rem; margin-top: 0.5rem; }
  .project-link {
    display: inline-flex; align-items: center; gap: 0.3rem;
    font-size: 0.8rem; font-weight: 500; color: var(--text-secondary);
    transition: color var(--transition-fast);
  }
  .project-link:hover { color: var(--purple-400); }
  .projects-more { text-align: center; margin-top: 2.5rem; }

  .blog-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 1.25rem; }
  .blog-card { display: flex; flex-direction: column; gap: 0.5rem; cursor: pointer; }
  .blog-card time { font-size: 0.75rem; color: var(--text-tertiary); font-weight: 500; }
  .blog-card-title { font-size: 1.05rem; font-weight: 700; color: var(--text-primary); line-height: 1.4; transition: color 0.3s ease; }
  .blog-card:hover .blog-card-title { color: var(--purple-400); }
  .blog-card-desc { font-size: 0.82rem; color: var(--text-tertiary); line-height: 1.6; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
  .blog-card-tags { display: flex; gap: 0.35rem; margin-top: auto; }

  .timeline { position: relative; padding-left: 2rem; }
  .timeline-item { display: flex; gap: 1.5rem; margin-bottom: 2rem; }
  .timeline-marker { display: flex; flex-direction: column; align-items: center; flex-shrink: 0; width: 20px; }
  .timeline-dot {
    width: 14px; height: 14px; border-radius: 50%;
    background: var(--bg-base); border: 2px solid var(--purple-500); flex-shrink: 0; z-index: 1;
  }
  .dot-current {
    background: var(--purple-500);
    box-shadow: 0 0 12px rgba(168,85,247,0.5);
  }
  .timeline-line { width: 2px; flex: 1; background: var(--glass-border); margin-top: 4px; }
  .timeline-content { flex: 1; }
  .current-badge {
    display: inline-block;
    padding: 0.2rem 0.7rem; margin-bottom: 0.5rem;
    font-size: 0.7rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em;
    background: rgba(168,85,247,0.15); border: 1px solid rgba(168,85,247,0.3);
    border-radius: var(--radius-full); color: var(--purple-400);
  }
  .exp-role { font-size: 1.15rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.35rem; }
  .exp-meta { display: flex; align-items: center; gap: 0.5rem; font-size: 0.85rem; color: var(--text-secondary); margin-bottom: 0.25rem; flex-wrap: wrap; }
  .exp-company { color: var(--purple-400); font-weight: 600; }
  .exp-divider { color: var(--text-tertiary); }
  .exp-meta-sub { display: flex; align-items: center; gap: 0.5rem; font-size: 0.8rem; color: var(--text-tertiary); margin-bottom: 0.75rem; }
  .exp-type-badge {
    padding: 0.15rem 0.5rem; font-size: 0.7rem; font-weight: 500;
    background: var(--glass-bg-light); border: 1px solid var(--glass-border);
    border-radius: var(--radius-full); color: var(--text-secondary);
  }
  .exp-desc { font-size: 0.85rem; color: var(--text-secondary); margin-bottom: 0.75rem; line-height: 1.6; }
  .exp-achievements { padding-left: 1.25rem; margin-bottom: 1rem; }
  .exp-achievements li { font-size: 0.82rem; color: var(--text-secondary); line-height: 1.6; margin-bottom: 0.4rem; }
  .exp-achievements li::marker { color: var(--purple-400); }
  .exp-tags { display: flex; flex-wrap: wrap; gap: 0.35rem; }

  .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; align-items: start; }
  .contact-left { display: flex; flex-direction: column; gap: 1.5rem; }
  .contact-cards { display: flex; flex-direction: column; gap: 0.75rem; }
  .contact-info-card {
    display: flex; flex-direction: column; gap: 0.2rem; cursor: pointer;
  }
  .contact-card-label { font-size: 0.75rem; color: var(--text-tertiary); font-weight: 500; text-transform: uppercase; letter-spacing: 0.05em; }
  .contact-card-value { font-size: 1rem; font-weight: 500; color: var(--text-primary); }
  .contact-socials-title { font-size: 0.9rem; font-weight: 600; color: var(--text-primary); margin-bottom: 0.75rem; }
  .contact-social-icons { display: flex; gap: 0.6rem; }
  .social-icon-lg {
    display: flex; align-items: center; justify-content: center;
    width: 44px; height: 44px; border-radius: var(--radius-lg);
    background: var(--glass-bg); border: 1px solid var(--glass-border);
    color: var(--text-secondary); transition: all var(--transition-fast);
  }
  .social-icon-lg:hover {
    color: var(--purple-400); border-color: rgba(168,85,247,0.3);
    background: rgba(168,85,247,0.1); transform: scale(1.05);
  }
  .why-card { padding: 1.5rem; }
  .why-title { font-size: 1rem; font-weight: 700; margin-bottom: 0.75rem; color: var(--text-primary); }
  .why-list { padding-left: 1.25rem; }
  .why-list li { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.7; margin-bottom: 0.3rem; }
  .why-list li::marker { color: var(--purple-400); }

  .contact-form-card { padding: 2rem; }
  .form-title { font-size: 1.25rem; font-weight: 700; margin-bottom: 0.5rem; }
  .form-desc { font-size: 0.85rem; color: var(--text-secondary); margin-bottom: 1.5rem; }
  .contact-form { display: flex; flex-direction: column; gap: 1.25rem; }
  .form-field { display: flex; flex-direction: column; gap: 0.4rem; }
  .form-field label { font-size: 0.8rem; font-weight: 600; color: var(--text-secondary); }
  .form-field input,
  .form-field textarea {
    padding: 0.75rem 1rem;
    background: var(--glass-bg-light);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-md);
    color: var(--text-primary);
    font-family: inherit;
    font-size: 0.9rem;
    outline: none;
    transition: border-color var(--transition-fast);
  }
  .form-field input::placeholder,
  .form-field textarea::placeholder { color: var(--text-tertiary); }
  .form-field input:focus,
  .form-field textarea:focus { border-color: rgba(168,85,247,0.4); }
  .form-field textarea { resize: vertical; min-height: 100px; }
  .form-submit { width: 100%; justify-content: center; }

  @media (max-width: 900px) {
    .about-grid { grid-template-columns: 1fr; }
    .contact-grid { grid-template-columns: 1fr; }
  }
  @media (max-width: 640px) {
    .hero { min-height: 90vh; }
    .hero-actions { flex-direction: column; }
    .projects-grid { grid-template-columns: 1fr; }
    .blog-grid { grid-template-columns: 1fr; }
    .skills-bars { grid-template-columns: 1fr; }
    .section-header-row { flex-direction: column; align-items: flex-start; }
    .timeline { padding-left: 0; }
    .timeline-marker { display: none; }
  }
</style>
